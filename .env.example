# Base path for your persistent data (keep trailing slash)
# Example: /media/
ARRPATH=/media/

# Global variables
TZ=America/Mexico_City

# Match these to your WSL/Linux user (run: id -u && id -g)
PUID=1000
PGID=1000

# Docker Group ID (run: getent group docker | cut -d: -f3)
# Needed for services that interact with Docker socket (like Homepage)
DOCKER_GID=971

# API Keys (Get these from each service's Settings -> General -> Security)
# Note: Variables must start with HOMEPAGE_VAR_ to be accessible in homepage config
HOMEPAGE_VAR_SONARR_API_KEY=
HOMEPAGE_VAR_RADARR_API_KEY=
HOMEPAGE_VAR_PROWLARR_API_KEY=
HOMEPAGE_VAR_JELLYFIN_API_KEY=

# Homepage Widget Credentials
HOMEPAGE_VAR_QBITTORRENT_USERNAME=admin
HOMEPAGE_VAR_QBITTORRENT_PASSWORD=
